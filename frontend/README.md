# Frontend для мобильного Mini App

Это frontend-часть мобильного приложения (mini app), созданная с использованием Next.js, Tailwind CSS и Shadcn/ui. Проект настроен с учетом архитектуры для мобильных устройств, включая кастомную анимацию переходов между страницами.

## 🚀 Начало работы

Для запуска проекта на вашем локальном компьютере выполните следующие шаги.

### Требования

-   Node.js (версия 20.x или выше)
-   npm или yarn

### Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [адрес вашего репозитория]
    cd [название папки проекта]
    ```

2.  **Установите зависимости:**
    ```bash
    cd frontend
    npm install
    ```

3.  **Запустите сервер для разработки:**
    ```bash
    npm run dev
    ```

После этого откройте [http://localhost:3000](http://localhost:3000) в вашем браузере, чтобы увидеть приложение.

## 🌿 Ветки проекта

Проект использует две ветки для разных режимов работы:

### `main` ветка - Динамический режим (Development)

- **Назначение**: Разработка и локальная работа
- **Режим**: Обычный Next.js с серверным рендерингом
- **Запуск**: `npm run dev` (работает без дополнительных настроек)
- **Особенности**:
  - Поддержка динамических маршрутов в runtime
  - Полная функциональность Next.js (SSR, API routes и т.д.)
  - Быстрая разработка без ограничений статического экспорта

### `deploy` ветка - Статический режим (Production)

- **Назначение**: Деплой на GitHub Pages
- **Режим**: Статический экспорт (`output: 'export'`)
- **Запуск**: Автоматически через GitHub Actions при push в ветку `deploy`
- **Особенности**:
  - Все страницы генерируются статически на этапе сборки
  - Требует `generateStaticParams()` для всех динамических маршрутов
  - Оптимизирован для хостинга на GitHub Pages

### Как это работает

Конфигурация автоматически переключается между режимами на основе переменной окружения `STATIC_EXPORT`:

- **main ветка**: `STATIC_EXPORT` не установлена → динамический режим
- **deploy ветка**: `STATIC_EXPORT=true` (устанавливается в CI/CD) → статический экспорт

### Работа с ветками

**Для разработки (main ветка):**
```bash
git checkout main
cd frontend
npm run dev
```

**Для деплоя (deploy ветка):**
```bash
# Создать/переключиться на deploy ветку
git checkout -b deploy
# Или если ветка уже существует
git checkout deploy

# Слить изменения из main
git merge main

# Запушить для автоматического деплоя
git push origin deploy
```

GitHub Actions автоматически соберет статический сайт и задеплоит его на GitHub Pages.

## 🛠️ Основные технологии

-   **Next.js 16 (App Router)**: Фреймворк для создания React-приложений с серверным рендерингом и лучшими практиками.
-   **Tailwind CSS**: Utility-first CSS фреймворк для быстрой и гибкой стилизации.
-   **Shadcn/ui**: Коллекция переиспользуемых UI-компонентов, которые вы полностью контролируете.
-   **Framer Motion**: Библиотека для создания плавной анимации.
-   **TypeScript**: Для статической типизации и улучшения опыта разработки.

## 📁 Структура проекта

Проект использует App Router от Next.js. Вот ключевые директории:

-   `app/(main)/`: **Основная рабочая область**. Это [группа маршрутов](https://nextjs.org/docs/app/building-your-application/routing/route-groups), которая не влияет на URL.
    -   `layout.tsx`: **Главный макет приложения**. Здесь определяется основной контейнер, "сцена" для анимаций и подключается нижнее меню. Все страницы внутри `(main)` будут использовать этот макет.
    -   `page.tsx`: **Главная страница** (`/`).
    -   `chat/page.tsx`: Страница чата (`/chat`).
    -   `profile/page.tsx`: Страница профиля (`/profile`).
    -   `category/[slug]/page.tsx`: **Динамическая страница** для отображения конкретной категории услуг (`/category/some-id`).

-   `components/`: Здесь лежат все переиспользуемые React-компоненты.
    -   `ui/`: Компоненты, добавленные через **Shadcn/ui** (например, `button.tsx`). Вы можете и должны редактировать их напрямую.
    -   `Header.tsx`, `BottomMenu.tsx`: Глобальные компоненты приложения.

-   `context/`: React Context для глобального состояния.
    -   `AnimationContext.tsx`: Управляет направлением анимации переходов между страницами.

-   `lib/`: Вспомогательные утилиты и работа с данными.
    -   `data.ts`: Мок-данные для приложения. **В будущем здесь будут запросы к API.**
    -   `utils.ts`: Утилита `cn` от Shadcn для объединения классов Tailwind.

## 🎨 Стилизация с Tailwind CSS

В проекте используется utility-first подход для стилизации. Вместо написания отдельного CSS вы добавляете классы напрямую в JSX/TSX.

```tsx
<h1 className="text-2xl font-bold text-primary">Мой заголовок</h1>
```

### Рекомендации

-   **VSCode Расширение**: Настоятельно рекомендуется установить расширение **[Tailwind CSS IntelliSense](https://marketplace.visualstudio.com/items?itemName=bradlc.vscode-tailwindcss)**. Оно обеспечивает автодополнение, подсветку синтаксиса и отображение итогового CSS при наведении.
-   **Документация**: Если вы не можете найти нужный класс, обратитесь к [официальной документации Tailwind CSS](https://tailwindcss.com/docs).

## 🧩 Работа с компонентами (Shadcn/ui)

**Важно:** Shadcn/ui — это не библиотека компонентов в привычном смысле. Вы не устанавливаете ее как зависимость. Вместо этого вы **копируете код компонентов прямо в ваш проект**. Это дает вам полный контроль над их стилями и поведением.

### Как добавить новый компонент

1.  Найдите нужный компонент на сайте [shadcn/ui](https://ui.shadcn.com/docs/components/button).
2.  Выполните команду в терминале, чтобы добавить его в проект. Например, для добавления `Card`:
    ```bash
    npx shadcn-ui@latest add card
    ```
3.  После этого в папке `components/ui/` появится новый файл `card.tsx`, который вы можете импортировать и использовать.

### Как кастомизировать компонент и создавать вариации

Так как код компонента находится в вашем проекте, вы можете его изменять как угодно. Давайте рассмотрим на примере создания новой вариации для кнопки.

**Задача:** Создать большую кнопку "Brand" с градиентом.

1.  **Откройте файл `components/ui/button.tsx`**.

2.  **Найдите объект `buttonVariants`**. Он использует библиотеку `class-variance-authority` для управления вариантами.

3.  **Добавьте новый `variant` и новый `size`:**

    ```typescript
    const buttonVariants = cva(
      "inline-flex items-center justify-center ...", // базовые стили
      {
        variants: {
          variant: {
            default: "bg-primary text-primary-foreground hover:bg-primary/90",
            destructive: "...",
            outline: "...",
            secondary: "...",
            ghost: "...",
            link: "...",
            // 👇 Наша новая вариация
            brand: "bg-gradient-to-r from-purple-500 to-pink-500 text-white font-bold",
          },
          size: {
            default: "h-9 px-4 py-2",
            sm: "...",
            lg: "...",
            icon: "...",
            // 👇 Наш новый размер
            xl: "h-14 rounded-md px-10",
          },
        },
        defaultVariants: {
          variant: "default",
          size: "default",
        },
      }
    )
    ```

4.  **Используйте новую вариацию в вашем коде:**

    ```tsx
    import { Button } from '@/components/ui/button';

    <Button variant="brand" size="xl">
      Наша новая крутая кнопка
    </Button>
    ```

Этот подход позволяет держать все стили в рамках Tailwind и легко расширять палитру компонентов под нужды проекта.